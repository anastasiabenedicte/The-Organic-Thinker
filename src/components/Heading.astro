---
// Denne komponent videresender kun den indkomne class-attribut.
// Dermed bruges kun de CSS-klasser du selv definerer (f.eks. .h1, .h2, .h3).
const { htmlheading = "h2" } = Astro.props;
const Htmlheading = htmlheading;
---
<Htmlheading class={Astro.props.class}><slot /></Htmlheading>